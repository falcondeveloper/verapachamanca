# VeraPachamanca Changes — 2026-08-25

- Added family access page requiring code `1517`.
- Added one-year family-access cookie so visitors do not re-enter the code on each visit.
- Added `robots.txt`, page `noindex` metadata, and Vercel `X-Robots-Tag` headers.
- Replaced the login-under-construction page with MySQL-backed username/password login.
- Added persistent `vera_sessions` login cookie with no normal expiration.
- No self-service password change, reset, registration, or recovery.
- Added a separate Volunteers menu/page.
- Anyone who has passed the family-access code may view, add, edit, and delete volunteer records.
- Added Vercel API functions for MySQL login, sessions, events, and volunteers.
- Added a MySQL connection health endpoint at `/api/health`.
- Added current 2026 event seed SQL for the Volunteers page.
- Preserved the latest homepage changes from the uploaded GitHub ZIP.
- Archived obsolete PostgreSQL/Supabase SQL files under `docs/old-postgres/`.

## Working photo upload completed
- Logged-in users can now select multiple photos on `upload.html`.
- Browser resizes each photo to a maximum dimension of 1600px and converts it to JPEG before upload.
- `/api/upload-photo` uploads the resized image by SFTP to GoDaddy:
  `/home/fi4begjkbozm/public_html/truecontractingsolutions.app/images/veras`
- Public image URL format is exactly:
  `https://truecontractingsolutions.app/images/veras/<image-name>`
- Upload metadata is inserted into `vera_photos` in GoDaddy MySQL.
- `gallery.html` loads database-uploaded photos and allows the uploader/admin to edit notes/year or delete the upload.
- `photos.html` adds database photo years/counts automatically.
- `photo_year = 0` means "Before 1980" in the existing SMALLINT column.
- Added exact dependency `ssh2-sftp-client` 12.1.1.


## 2026-08-26 - Simplified user selection login
- Removed username/password login from the website.
- Login page now loads active `vera_users` into a dropdown.
- Selecting a family member creates the same persistent `vera_sessions` session.
- Added `/api/users`.
- Password values are no longer read by the application.
- Added `database/04_MAKE_PASSWORD_OPTIONAL.sql` for existing databases.
